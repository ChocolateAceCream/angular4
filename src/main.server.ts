import { enableProdMode } from '@angular/core';

//this will reference the main.server.ts file as an entry point of our app
export { AppServerModule } from './app/app.server.module';

enableProdMode();
